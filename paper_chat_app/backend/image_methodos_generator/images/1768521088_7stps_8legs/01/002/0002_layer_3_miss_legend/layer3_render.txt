silently
Create a highly detailed academic infographic in a clean, professional style for a single-page landscape canvas (wide format, 16:9 aspect ratio). Use a modern sans-serif font like Helvetica or Arial for all text, with bold titles in 24-28pt, step labels in 20pt, bullet points in 14-16pt, and small labels in 12pt. Employ a subtle gradient background from light blue (#E3F2FD) on the left to light gray (#F5F5F5) on the right, with thin white drop shadows on all boxes for depth. Divide the canvas into three distinct horizontal regions: left third for initialization (R_Init, pale green accent #E8F5E8), wide center two-thirds for the iterative loop (R_Loop, pale blue accent #E3F2FD with dashed boundary to indicate looping), and right third for final evaluation (R_Final, pale orange accent #FFF3E0). Use rounded rectangular boxes (20px corner radius) for all steps, sized proportionally: S1 and S7 medium (200x150px), loop steps slightly smaller (180x140px), arranged in a left-to-right workflow with vertical alignment in the center loop (S2 top, S3, S4, S5 middle, S6 bottom).

**Main Title:** Center at the top in large bold text (36pt, navy blue #1976D2): "ProTeGi (Prompt Optimization with Textual Gradients)". Subtitle below (18pt): "Beam search methodology with 6 optimization cycles, b=4 beam width".

**Step Boxes (exact content, positioned as follows):**
- **S1 (top-left in R_Init, green #4CAF50 fill, white text):** Label "Step 1: Initialization and Setup" with 🌱 icon. Bullets: "seed prompt p₀", "beam width b=4", "6 optimization cycles".
- **S2 (top-center in R_Loop, blue #2196F3 fill, white text):** Label "Step 2: Error Identification" with ❌ icon. Bullets: "minibatch of 64 examples", "gpt-3.5-turbo", "groups of 4 errors".
- **S3 (upper-middle-center in R_Loop, yellow #FFC107 fill, black text):** Label "Step 3: Generating Textual Gradients" with 🧠 icon. Bullets: "feedback prompt ∆", "m=4 textual gradients (g)", "LLM as critic".
- **S4 (middle-center in R_Loop, purple #9C27B0 fill, white text):** Label "Step 4: Editing the Prompt with Gradient Feedback" with 📝 icon. Bullets: "editing prompt δ", "4 successor prompts (p′) per gradient", "random sample of 8 candidates".
- **S5 (lower-middle-center in R_Loop, purple #9C27B0 fill, white text):** Label "Step 5: Expanding the Search with Paraphrasing" with 🔄 icon. Bullets: "paraphrasing LLM", "p=2 paraphrases (p″) per successor", "candidate pool".
- **S6 (bottom-center in R_Loop, red #F44336 fill, white text):** Label "Step 6: Selecting the Best Prompts with a Bandit Algorithm" with 🎰 icon. Bullets: "best arm identification", "UCB-E or Successive Rejects", "F1 score estimation", "select top 4 for new beam".
- **S7 (top-right in R_Final, brown #795548 fill, white text):** Label "Step 7: Iteration and Final Selection" with 🔄 icon (loop style). Bullets: "6 optimization steps", "held-out test set of 150 examples", "3 experiment repeats", "average F1 score".

**Arrows and Connections (thick 4px arrows with gradient stroke from box color to white, arrowheads, exact paths):**
- Straight horizontal arrow from S1 right edge to S2 left edge, label above "Start Loop" (black text, italic).
- Straight downward-right arrow from S2 bottom to S3 top.
- Straight downward-right arrow from S3 bottom to S4 top.
- Straight downward-right arrow from S4 bottom to S5 top.
- Straight upward-right arrow from S5 bottom to S6 top.
- Thick curved clockwise loop arrow from S6 right edge, arching up and left over the loop boxes back to S2 left edge, bold label "Repeat 6 times" along the curve (white text in red bubble).
- Straight downward-right arrow from S6 bottom-right to S7 top-left, label "After final iteration" (black text).

**Callouts (small curved speech bubbles with thin lines pointing to exact steps, 12pt text, semi-transparent white background):**
- From S1: "Beam width b=4".
- From S2: "Minibatch: 64 examples".
- From S3: "LLM as critic generates m=4 gradients".
- From S5: "Candidate pool expanded with p=2 paraphrases per successor".
- From S6: "Selects top 4 prompts for the new beam".
- From S7: "Final validation on 150 test examples".

**Legend (bottom-center, compact horizontal row of 7 icons with labels in small 10pt text, matching colors):**
🌱 seed prompt: Initial human-written prompt with task description and two fixed few-shot examples.
📊 beam: A collection of the current best-performing prompts, with a fixed size (b=4).
❌ errors: Groups of 4 incorrect classifications made by a prompt on the minibatch.
🧠 textual gradients: Natural language explanations for why a prompt failed, generated by an LLM acting as a critic.
📝 successor prompts: New prompt candidates generated by rewriting an old prompt based on a textual gradient.
🔄 paraphrases: Semantically similar but differently worded variations of successor prompts.
🎰 bandit algorithm: An efficient selection method (e.g., UCB-E) to identify the best prompts from a large pool of candidates with a limited evaluation budget.

**Accuracy Rules (strictly enforce):**
- Do not alter numbers: beam b=4, 6 cycles, minibatch 64, errors groups of 4, m=4 gradients, 8 successors sampled, p=2 paraphrases, test set 150 examples, 3 repeats.
- This is explicitly a beam search process; include subtle "Beam Search" badge on S1 and S6.
- No invented steps, icons, or text—use only provided labels, bullets, edges, and legend verbatim (use Unicode subscripts: p₀, p′, p″, ∆, δ).
- Ensure loop visually encircles S2-S6 clearly but does not overlap text.
- Balance spacing: even gaps between boxes (30px), no crowding, high readability.
- Professional academic tone: no cartoons, realistic icons as specified, subtle glows on arrows for flow emphasis.