Create a highly detailed academic infographic in landscape orientation for a single-page canvas, depicting the OPRO (Optimization by PROmpting) Methodology as a professional workflow diagram. Use a clean, modern visual style with sans-serif fonts (e.g., Arial or Helvetica), subtle gradients, rounded rectangles for nodes, high contrast for readability, and a white background with light gray grid lines for alignment. Employ a vibrant yet professional color palette: setup_and_data nodes in #1E88E5 (blue), optimization_loop_actions in #FFC107 (amber), evaluation_and_scoring in #D81B60 (pink), finalization_and_testing in #43A047 (green). Arrows are thick (4-6pt), curved where specified, with white text labels in bold black outlines. Include small icons: brain-circuit for optimizer, clipboard-check for scorer, lightbulb-on for generation, database-sync for update, shield-check for validation. Ensure precise spatial layout: top-to-bottom linear flow transitioning to a prominent iterative loop on the right, with final linear exit at bottom.

**Title**: Place "OPRO (Optimization by PROmpting) Methodology" at the top center in large bold font (48pt, dark blue #1E88E5), spanning 60% width, with subtle glow shadow.

**Layout Regions** (divide canvas into these exact positions with invisible boundaries):
- **R_SETUP (top_left, 25% height x 30% width)**: Linear vertical stack containing S1 at top and S2 below.
- **R_META_PROMPT (left, 20% height x 15% width, centered vertically below R_SETUP)**: Single pivot node S3.
- **R_OPTIMIZATION_LOOP (right, 40% height x 45% width, aligned to right side)**: Large enclosing dashed golden rectangle labeled "Step 4: Iterative Optimization Loop" (S4 as title header in amber #FFC107, 72pt). Inside clockwise: S4.1 top, S4.2 right, S4.3 bottom.
- **R_FINALIZATION (bottom_center, 15% height x 40% width)**: Linear horizontal stack with S5 left, S6 right.

**Nodes** (all as rounded rectangles, 200x120px min, with exact labels as bold 24pt headers, key_components as bullet points in 14pt, icons top-right, callouts as attached speech bubbles in yellow with black text):
- **S1 (top of R_SETUP, blue #1E88E5)**: "Step 1: Problem Setup and Dataset Preparation" (setup type). Bullets: "task definition", "optimizer LLM selection", "scorer LLM selection", "dataset split".
- **S2 (below S1, blue #1E88E5)**: "Step 2: Generate Initial Solution Trajectory" (data_processing). Bullets: "initial prompts", "scorer evaluation", "initial solution-score pairs".
- **S3 (R_META_PROMPT, blue #1E88E5)**: "Step 3: Construct the Meta-Prompt" (data_processing). Bullets: "optimization trajectory", "problem examples", "meta-instructions".
- **S4 (header over R_OPTIMIZATION_LOOP, large amber #FFC107 banner)**: "Step 4: Iterative Optimization Loop" (iterative_optimization). Bullets: "optimizer LLM", "scorer LLM", "trajectory update". Encloses substeps.
- **S4.1 (top of loop, amber #FFC107)**: "Substep 4.1: Generate New Candidate Solutions" (iterative_optimization). Bullets: "meta-prompt", "high temperature (1.0)", "batch of 8 solutions". Icon: lightbulb-on. Callout bubble: "Optimizer generates a batch of 8 new solutions with high creativity (temp=1.0)."
- **S4.2 (right of loop, pink #D81B60)**: "Substep 4.2: Evaluate New Solutions" (evaluation). Bullets: "training subset", "scorer LLM", "accuracy scores". Icon: clipboard-check.
- **S4.3 (bottom of loop, blue #1E88E5)**: "Substep 4.3: Update the Optimization Trajectory" (data_processing). Bullets: "top 20 pruning", "trajectory maintenance". Icon: database-sync. Callout bubble: "Trajectory is pruned to always maintain the top 20 best-performing solutions."
- **S5 (left of R_FINALIZATION, green #43A047)**: "Step 5: Select the Optimal Solution" (finalization). Bullets: "highest score solution", "complete trajectory review".
- **S6 (right of R_FINALIZATION, green #43A047)**: "Step 6: Final Validation on Test Set" (testing). Bullets: "held-out test set", "deterministic scoring (temp=0)". Icon: shield-check. Callout bubble: "Final performance is measured on a held-out test set using deterministic scoring (temp=0)."

**Arrows** (bold, follow exact styles, labels mid-arrow in 16pt white-on-black):
- S1 to S2: straight vertical down, label "prepared models and data".
- S2 to S3: straight diagonal down-right, label "initial trajectory".
- S3 to S4.1: straight horizontal right, label "meta-prompt".
- S4.1 to S4.2: clockwise_loop_segment (curved right-down), label "new candidates".
- S4.2 to S4.3: clockwise_loop_segment (curved down-left), label "new scores".
- S4.3 to S3: return_arrow (curved upward loop-back left, thick dashed for emphasis), label "updated trajectory".
- S4 (bottom of loop box) to S5: downward_exit (straight down then left curve), label "loop termination".
- S5 to S6: straight horizontal right, label "optimal solution".

**Legend** (bottom-right corner, 20% width x 10% height, compact table with color swatches):
- optimizer: "LLM designated to generate and refine solutions" (brain-circuit icon).
- scorer: "LLM used to evaluate solution performance" (clipboard-check icon).
- optimization trajectory: "History of solution-score pairs, maintained as top 20 best-performing".
- meta-prompt: "Comprehensive prompt containing trajectory, problem examples, and meta-instructions".
- training subset: "Small dataset portion (e.g., 3.5% of training set) for efficient scoring".

**Accuracy Rules** (do not deviate): Exactly match node IDs/labels/bullets/edges. Maintain top 20 trajectory size, batch of 8 candidates, temp=1.0 for generation, training subset for optimization (not full dataset), test set only for S6 validation, optimizer generates/scorer evaluates. No invented steps, substeps, or changes to flow. Loop is strictly S3 ↔ S4.1-S4.2-S4.3. Professional spacing, no overlaps, high legibility at A4 print size.