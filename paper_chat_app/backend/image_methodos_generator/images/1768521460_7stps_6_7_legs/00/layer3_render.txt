Create a highly detailed academic infographic in a single-page landscape canvas (wide format, 16:9 aspect ratio) illustrating the "Prompt Optimization with Textual Gradients (ProTeGi)" methodology as a professional workflow diagram. Use a clean, modern academic style with sans-serif fonts (e.g., Helvetica or Arial), subtle drop shadows on boxes, high contrast for readability, and a professional color palette: background is light gray (#F5F5F5), with color-coded boxes per node type—setup_final nodes in green (#4CAF50), optimizer_action in blue (#2196F3), scorer_action in amber (#FFC107), loop_control in purple (#9C27B0). Include precise icons for each step: S1 (🎯), S2 (∇), S3 (δ), S4 (✨), S5 (📊), S6 (🔄), S7 (🏆). Position the main title "Prompt Optimization with Textual Gradients (ProTeGi)" prominently at the top center in large bold font (48pt, black).

Divide the canvas into three distinct horizontal regions separated by faint vertical gray lines:

- Left region (R_Initialization, 25% width): Contains only S1 box vertically centered.
- Center region (R_Optimization_Loop, 50% width): Dominant iterative loop area with S2, S3, S4, S5, S6 arranged in a clockwise loop formation—S2 top, S3 right, S4 bottom-right, S5 bottom-left, S6 left side.
- Right region (R_Finalization, 25% width): Contains only S7 box vertically centered.

Each step is a rounded rectangle box (200x120px) with icon top-left (48px), bold step label (24pt) above, and 3-4 bullet points below listing exact key_components in 14pt font, bullet-style, no additions or changes:

- S1 "Step 1: Initialization with a Starting Prompt" (green): • p₀ (initial prompt) • training dataset • beam
- S2 "Step 2: Generating Textual 'Gradients'" (blue): • random minibatch • ∇ prompt • LLM • g (textual gradient)
- S3 "Step 3: Editing Prompts via 'Gradient Descent'" (blue): • δ prompt • current prompt + g • LLM • improved prompt candidates
- S4 "Step 4: Expanding the Search Space" (blue): • gradient editing (S2+S3) • paraphrasing • Monte Carlo sampling
- S5 "Step 5: Efficiently Selecting the Best Candidates" (amber): • best arm identification • Successive Rejects / UCB Bandits • minimal LLM API calls
- S6 "Step 6: Iterating and Updating the Beam" (purple): • update beam with b best prompts • fixed number of iterations
- S7 "Step 7: Final Prompt Selection" (green): • evaluate all prompts on final beam • F1 score • p* (final optimized prompt)

Arrows are thick (4pt), curved where specified, with white text labels in black-outlined boxes, following exact paths without alteration:
- Straight arrow from S1 right to S2: label "Begin optimization loop"
- Straight arrow from S2 down to S3: no label
- Straight arrow from S3 right to S4: no label
- Straight arrow from S4 left to S5: no label
- Straight arrow from S5 up to S6: label "Identify new beam"
- Bold dashed clockwise loop arrow encircling S2-S3-S4-S5-S6 from S6 back to S2: label "Repeat for fixed iterations" (prominently curved around the loop)
- Straight downward-then-right exit arrow from S6 to S7: label "After final iteration"

Add four curved callout bubbles (yellow background, black text, thin leader lines):
- Attach to S2: "The ∇ prompt generates a 'textual gradient' (g), a natural language summary of the prompt's flaws."
- Attach to S3: "The δ prompt acts like gradient descent, editing the prompt in the 'opposite semantic direction' of the flaw summary."
- Attach to S5: "Uses bandit algorithms to efficiently find the best prompts while minimizing costly LLM API calls."
- Attach to S6: "The beam is updated with the top 'b' candidates, maintaining a diverse set of high-quality prompts for the next iteration."

Bottom-left corner: Legend box (300x200px, white background) with 5 rows of symbols and exact descriptions:
• p₀: The initial, human-written prompt that starts the process.
• b: The beam size, representing the number of best candidate prompts kept at each iteration.
• ∇ (nabla): An LLM prompt designed to analyze errors and generate a textual summary of a prompt's flaws.
• g: The textual 'gradient', which is the natural language summary of a prompt's flaws generated by the ∇ prompt.
• δ (delta): An LLM prompt that takes a current prompt and its textual gradient (g) to generate improved prompt candidates.
• beam: A list that holds the top 'b' candidate prompts at each stage of optimization.

Rules for accuracy—do not invent, omit, or alter any element: Maintain iterative beam search structure exactly; textual gradients (g) drive all improvements as natural language flaw summaries; editing uses LLM to move opposite semantic direction of gradient; selection uses bandit algorithms (Successive Rejects/UCB) for efficiency; loop repeats fixed iterations updating top b prompts; final p* selected by F1 on full beam. No extra text, steps, or changes to labels/components. Ensure mathematical symbols render precisely: p₀, ∇, g, δ, p*. High-resolution, vector-sharp, print-ready quality.