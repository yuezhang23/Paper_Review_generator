fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
openai==1.3.0
openreview-py>=1.0.0
httpx==0.25.0
requests>=2.31.0  # HTTP library for GROBID API calls
pydantic==2.5.0
websockets==12.0
PyPDF2==3.0.1
python-multipart==0.0.6
google-genai>=0.2.0

# Summary generator dependencies (RAG pipeline)
faiss-cpu>=1.7.4  # Vector index for RAG (use faiss-gpu for GPU support)
FlagEmbedding>=1.2.0  # BGE embedding models
camelot-py[cv]>=0.11.0  # Table extraction from PDFs
tabula-py>=2.5.1  # Table extraction fallback
PyMuPDF>=1.23.0  # PDF processing (fitz) for figure extraction
pytesseract>=0.3.10  # OCR for figure text extraction
Pillow>=10.0.0  # Image processing
lxml>=4.9.0  # XML parsing for GROBID output
numpy>=1.24.0  # Numerical operations for embeddings
pandas>=2.0.0  # Data processing for tables

# Image editing dependencies (for image_methodos_generator)
diffusers>=0.30.0  # Diffusion models for image editing
peft>=0.17.0  # Parameter-Efficient Fine-Tuning (required by diffusers)
torch>=2.0.0  # PyTorch (required by diffusers)

# NOTE: If you have axolotl installed globally, it may conflict with peft>=0.17.0.
# Consider using a virtual environment for this project to avoid conflicts:
#   python -m venv venv
#   source venv/bin/activate  # On macOS/Linux
#   pip install -r requirements.txt
