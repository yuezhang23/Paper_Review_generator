"""
Backup LLM Orchestrator Server (FastAPI + LangGraph).

This module provides a drop-in replacement gateway for an upstream MCP/orchestrator
that routes requests to multiple LLM backends using a config file and an auth token.
"""

